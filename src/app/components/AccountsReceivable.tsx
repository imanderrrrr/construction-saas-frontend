/**
 * Cuentas por Cobrar.
 *
 * The screen itself lives in `accounts/ReceivablesScreen` — this file is the
 * name the two dashboards import (admin and finance both mount it lazily).
 */
export { ReceivablesScreen as AccountsReceivable } from './accounts/ReceivablesScreen';
