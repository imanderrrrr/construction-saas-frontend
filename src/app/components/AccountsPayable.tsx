/**
 * Cuentas por Pagar.
 *
 * The screen itself lives in `accounts/PayablesScreen` — this file is the name
 * the two dashboards import (admin and finance both mount it lazily).
 */
export { PayablesScreen as AccountsPayable } from './accounts/PayablesScreen';
